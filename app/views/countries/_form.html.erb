<%= form_for(@country) do |f| %>
  <%= render 'shared/error_messages', object: @country %>
  <div class="field">
    <%= f.label :name, "Имя" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Создать" %>
  </div>
<% end %>
